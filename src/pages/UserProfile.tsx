/**
 * Authenticated user profile page.
 *
 * Stub for now — the rich account / observation sites / followed objects
 * UI will be assembled in a follow-up iteration on top of these foundations.
 */
export function UserProfile() {
  return (
    <div className="min-h-screen bg-space-bg">
      <main className="max-w-5xl mx-auto px-4 sm:px-6 py-12">
        <h1 className="text-2xl font-semibold text-text-primary">Profile</h1>
        <p className="mt-2 text-text-secondary">
          Coming up: account info, observation sites, followed objects.
        </p>
      </main>
    </div>
  );
}
